using System;
using System.Reflection;

namespace Creshendo.Util
{
    public class BeanFilter
    {
        public PropertyInfo[] filter(PropertyInfo[] props)
        {
            return props;
        }
    }
}